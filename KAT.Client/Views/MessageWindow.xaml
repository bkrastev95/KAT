<controls:MetroWindow x:Class="KAT.Client.Views.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Icon="../Images/KAT.Icon.ico"
        Title="Съобщение:" Height="150" Width="500"
        ResizeMode="NoResize"
        ShowTitleBar="True"
        BorderThickness="1"
        Background="BlanchedAlmond"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontFamily" Value="Colibri"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Border Name="Border" BorderThickness="1">
        <Grid>
            <TextBlock TextWrapping="Wrap" Name ="MessageHolder" Style="{StaticResource LabelStyle}"></TextBlock>
        </Grid>
    </Border>
</controls:MetroWindow>
